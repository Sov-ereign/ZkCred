{
  "version": 2,
  "devCommand": "npm run ui",
  "buildCommand": "npm run ui:build",
  "outputDirectory": "ui/dist",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/index.js" }
  ]
}
